---
# tasks file for roles/nginx_proxy
- name: 安装nginx
  yum: 
    name: nginx
    state: present

- name: 推送代理配置
  template:
    src: nginx.conf.j2
    dest: /etc/nginx/nginx.conf
  notify: reload nginx

- name: 启动服务
  systemd:
    name: nginx
    state: started
    enabled: yes

